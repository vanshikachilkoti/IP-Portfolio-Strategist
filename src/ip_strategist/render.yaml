services:
  - type: web
    name: ip-strategist-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "flask run --host 0.0.0.0 --port $PORT"
    envVars:
      - key: FLASK_APP
        value: ip_strategist
